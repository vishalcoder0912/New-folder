{
  "name": "edtech-cms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin-panel",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin-panel",
    "build": "npm run build --prefix client && npm run build --prefix admin-panel",
    "seed": "npm run seed --prefix server"
  }
}
